node_modules/
dist/
.DS_Store
*.log
.playwright-mcp/
.astro/

# Client-sensitive material (proposals, pricing, contacts) — NEVER committed;
# these repos and their deployed sites are public.
/docs/private/

# Local-only state, 2026-09-24. The repo's lockfile is package-lock.json (the
# deploy script runs through npm); bun's lockfile is a per-machine convenience.
# Agent memory is Claude Code's per-machine scratch for the builder agents.
bun.lock
.claude/agent-memory/
