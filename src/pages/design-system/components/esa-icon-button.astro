---
import ComponentDoc from '../../../layouts/ComponentDoc.astro';
import Section from '@esa/docs/Section.astro';
import Preview from '@esa/docs/Preview.astro';
import EsaIconButton from '@esa/ecology/esa-icon-button.astro';

// Prose only — names, types and defaults are generated from the component
// source by src/data/component-api.ts, so this table cannot drift.
const describe = {
  icon: "Lucide icon name.",
  label: "Accessible name (aria-label + title). Required — an icon-only control needs a name.",
  href: "When present renders an <a>; otherwise a <button>.",
  size: "Square footprint on the shared form size scale.",
};
---
<ComponentDoc name="Icon Button" category="Deprecated" slug="esa-icon-button" type=".astro" status="deprecated"
  supersededBy={'<EsaButton variant="chrome" iconOnly>'}
  summary="Deprecated 2026-08-14, folded into <a href='/components/esa-button'>esa-button</a> (migrations.json: <code>icon-button-to-button-chrome</code>). What it was: an icon-only action for toolbars and chrome (search, close, overflow) — lightweight, transparent, and inheriting <code>currentColor</code>, which is now what <code>variant=&quot;chrome&quot;</code> means. <code>label</code> was required for accessibility; on the button it can only be a build-time warning, since the label is a slot.">

  <Section id="preview" title="Preview">
    <Preview code={`<EsaIconButton icon="search" label="Search" />`}>
      <EsaIconButton icon="search" label="Search" />
    </Preview>
  </Section>

  <Section id="sizes" title="Sizes">
    <Preview code={`<EsaIconButton icon="search" label="Search" size="xs" />
<EsaIconButton icon="search" label="Search" size="sm" />
<EsaIconButton icon="search" label="Search" size="md" />
<EsaIconButton icon="search" label="Search" size="lg" />`}>
      <div style="display:flex;gap:8px;align-items:center">
        <EsaIconButton icon="search" label="Search" size="xs" />
        <EsaIconButton icon="search" label="Search" size="sm" />
        <EsaIconButton icon="search" label="Search" size="md" />
        <EsaIconButton icon="search" label="Search" size="lg" />
      </div>
    </Preview>
  </Section>

  <Section id="oncontext" title="On any surface (currentColor)">
    <p>Transparent background and <code>currentColor</code> mean it reads correctly on a dark bar or a light surface; the hover tint is derived from the inherited color.</p>
    <Preview code={`<div style="color:#fff;background:#1e5386">
  <EsaIconButton icon="search" label="Search" />
</div>`}>
      <div style="display:flex;gap:16px">
        <div style="color:#fff;background:#1e5386;padding:8px 16px;border-radius:8px;display:flex;gap:4px">
          <EsaIconButton icon="search" label="Search" />
          <EsaIconButton icon="bell" label="Notifications" />
          <EsaIconButton icon="settings" label="Settings" />
        </div>
        <div style="background:#f3f7fc;padding:8px 16px;border-radius:8px;display:flex;gap:4px;color:#1e5386">
          <EsaIconButton icon="search" label="Search" />
          <EsaIconButton icon="x" label="Close" />
        </div>
      </div>
    </Preview>
  </Section>

  <Section id="api" title="API">
    <p class="api-note">
      Props, types, defaults and events are <strong>generated from the component
      source</strong> and documented on the hub — this page shows the component
      wearing the ProjectFirma 2.0 skin.
      <a href="https://esassoc.github.io/ecology/components/esa-icon-button" rel="noreferrer">ecology / components / esa-icon-button</a>
    </p>
  </Section>
</ComponentDoc>
