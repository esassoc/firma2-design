---
import ComponentDoc from '../../../layouts/ComponentDoc.astro';
import Section from '@esa/docs/Section.astro';
import Preview from '@esa/docs/Preview.astro';
import { withBase } from '@esa/docs/base';

// Prose only — names, types and defaults are generated from the component
// source by src/data/component-api.ts, so this table cannot drift.
const describe = {
  mode: "DEPRECATED — 'select' renders a button trigger over a fixed list, which is <a href='/components/esa-select'>esa-select</a>. Still honoured; warns at runtime.",
  "trigger-style": "DEPRECATED — only ever styled the mode='select' button trigger, so it goes with it.",
  options: "Selectable options. Set as a property.",
  name: "Form field name — the key this control submits under.",
  multiple: "Allow multiple selections with removable chips.",
  size: "Control size (reflected attribute).",
  label: "Field label above the control.",
  placeholder: "Placeholder for the trigger/input.",
  disabled: "Disables interaction (reflected attribute).",
  required: "Marks the field required.",
  "help-text": "Helper text below the control.",
  "error-text": "Error message; sets the error state when present.",
  loading: "Shows a spinner and \"Searching...\" state.",
  "debounce-ms": "Debounce window for the search event.",
  "results-count": "When set, renders \"Displaying X of Y results\".",
  value: "Property accessor. Single = string; multiple = string array.",
};

const events = [
  { name: 'change', type: 'CustomEvent<{ value: string | string[] }>', description: 'Fired on selection change. Composed and bubbles.' },
  { name: 'search', type: 'CustomEvent<{ term: string }>', description: 'Debounced search term (300ms default), de-duplicated. Composed and bubbles.' },
];
---
<ComponentDoc name="Combobox" category="Forms" slug="esa-combobox" type="wc" status="stable"
  summary="An autocomplete <strong>input</strong> with a list of suggestions. Filters as you type, highlights matches, and supports remote data through a debounced <code>search</code> event with loading and result-count states.">

  <Section id="preview" title="Preview">
    <Preview code={`<esa-combobox label="State" placeholder="Pick a state"></esa-combobox>`}>
      <esa-combobox label="State" placeholder="Pick a state"></esa-combobox>
    </Preview>
  </Section>

  <Section id="which" title="Combobox or Select?">
    <p><strong>If the user types into it, it is a Combobox. If they pick from what you
    gave them, it is a <a href={withBase('/components/esa-select')}>Select</a>.</strong></p>
    <ul class="a11y">
      <li><strong>Combobox</strong> — an input with suggestions. Filters as you type,
      highlights the match, and can fetch remotely: debounced <code>search</code>,
      <code>loading</code>, <code>results-count</code>.</li>
      <li><strong>Select</strong> — a button opens a list, no text entry. Distant
      options are reached by typeahead.</li>
    </ul>
    <p><code>mode</code> and <code>trigger-style</code> are <strong>deprecated</strong>.
    <code>mode="select"</code> rendered a button trigger over a fixed list — that is a
    Select, under this component's name — and it was the <em>default</em> until
    2026-08-15, so the default combobox was a select. Both still work and warn at
    runtime; <code>trigger-style</code> only ever styled that button trigger.</p>
  </Section>

  <Section id="sizes" title="Sizes">
    <Preview code={`<esa-combobox label="Extra small" size="xs"></esa-combobox>
<esa-combobox label="Small" size="sm"></esa-combobox>
<esa-combobox label="Medium" size="md"></esa-combobox>
<esa-combobox label="Large" size="lg"></esa-combobox>`}>
      <esa-combobox label="Extra small" size="xs"></esa-combobox>
      <esa-combobox label="Small" size="sm"></esa-combobox>
      <esa-combobox label="Medium" size="md"></esa-combobox>
      <esa-combobox label="Large" size="lg"></esa-combobox>
    </Preview>
  </Section>

  <Section id="states" title="States">
    <p>Multi-select renders chips. Loading, results-count, error, and disabled states are attribute-driven.</p>
    <Preview code={`<esa-combobox label="States visited" multiple placeholder="Add states"></esa-combobox>
<esa-combobox label="With results count" results-count="50" help-text="Shows 'Displaying X of Y results'"></esa-combobox>
<esa-combobox label="Loading" loading></esa-combobox>
<esa-combobox label="Error" error-text="Selection required"></esa-combobox>
<esa-combobox label="Disabled" disabled></esa-combobox>`}>
      <esa-combobox label="States visited" multiple placeholder="Add states"></esa-combobox>
      <esa-combobox label="With results count" results-count="50" help-text="Shows 'Displaying X of Y results'"></esa-combobox>
      <esa-combobox label="Loading" loading></esa-combobox>
      <esa-combobox label="Error" error-text="Selection required"></esa-combobox>
      <esa-combobox label="Disabled" disabled></esa-combobox>
    </Preview>
  </Section>

  <Section id="api" title="API">
    <p class="api-note">
      Props, types, defaults and events are <strong>generated from the component
      source</strong> and documented on the hub — this page shows the component
      wearing the ProjectFirma 2.0 skin.
      <a href="https://esassoc.github.io/ecology/components/esa-combobox" rel="noreferrer">ecology / components / esa-combobox</a>
    </p>
  </Section>

  <Section id="accessibility" title="Accessibility">
    <ul class="a11y">
      <li>Autocomplete input carries <code>role="combobox"</code> with <code>aria-expanded</code>, <code>aria-haspopup="listbox"</code>, and <code>aria-autocomplete="list"</code>.</li>
      <li>Panel is <code>role="listbox"</code>; options are <code>role="option"</code> with <code>aria-selected</code>.</li>
      <li>Keyboard: ArrowUp/Down navigate, Enter selects, Escape/Tab close. Outside-click closes.</li>
      <li>Matched substrings are wrapped in <code>&lt;mark&gt;</code> for visible highlight.</li>
      <li>Form-associated via <code>ElementInternals</code> — value submits with the enclosing form.</li>
    </ul>
  </Section>

</ComponentDoc>

<script>
  import '@esa/ecology/esa-combobox';
  const states = [
    'Alabama','Alaska','Arizona','Arkansas','California','Colorado','Connecticut',
    'Delaware','Florida','Georgia','Hawaii','Idaho','Illinois','Indiana','Iowa',
    'Kansas','Kentucky','Louisiana','Maine','Maryland','Massachusetts','Michigan',
    'Minnesota','Mississippi','Missouri','Montana','Nebraska','Nevada',
  ].map((s) => ({ label: s, value: s.toLowerCase() }));
  customElements.whenDefined('esa-combobox').then(() => {
    document.querySelectorAll('esa-combobox').forEach((el) => {
      (el as any).options = states;
    });
  });
</script>

<style>
  h3 { font-size: 16px; font-weight: 600; margin: var(--spacing-500, 1.5rem) 0 var(--spacing-200, 0.5rem); }
</style>
