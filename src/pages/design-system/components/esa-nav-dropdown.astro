---
import ComponentDoc from '../../../layouts/ComponentDoc.astro';
import Section from '@esa/docs/Section.astro';
import Preview from '@esa/docs/Preview.astro';
import EsaAppBar from '@esa/ecology/esa-app-bar.astro';
import EsaNavDropdown from '@esa/ecology/esa-nav-dropdown.astro';
import EsaLinkColumn from '@esa/ecology/esa-link-column.astro';

// Prose only — names, types and defaults are generated from the component
// source by src/data/component-api.ts, so this table cannot drift.
const describe = {
  label: "Trigger label.",
  icon: "Leading icon on the trigger.",
  size: "Trigger size (matches esa-icon-link).",
  align: "Which edge of the trigger the panel aligns to.",
};
---
<ComponentDoc name="Nav Dropdown" category="Navigation" slug="esa-nav-dropdown" type=".astro" status="stable"
  summary={"An <a href='/components/esa-icon-link'>Icon Link</a> trigger paired with a floating panel via a native <code>&lt;details&gt;</code> disclosure — zero runtime JS. The panel is slotted, so it holds any menu; the chevron rotates when open. Compose it inside an <a href='/components/esa-app-bar'>App Bar</a> for chrome nav."}>

  <Section id="preview" title="Preview (click to open)">
    <Preview align="start" code={`<EsaAppBar tone="brand" as="header">
  <EsaNavDropdown icon="trees" label="Mitigation work">
    <EsaLinkColumn heading="View all topics" links={[…]} />
  </EsaNavDropdown>
</EsaAppBar>`}>
      <EsaAppBar tone="brand" as="header">
        <EsaNavDropdown icon="trees" label="Mitigation work">
          <EsaLinkColumn heading="Mitigation work" links={[
            { label: 'Projects' }, { label: 'Contracts' }, { label: 'Portfolios' }, { label: 'Work elements' },
          ]} />
        </EsaNavDropdown>
        <EsaNavDropdown icon="trending-up" label="Reporting">
          <EsaLinkColumn heading="Reporting" links={[
            { label: 'Report Center' }, { label: 'Maps' }, { label: 'Publications' },
          ]} />
        </EsaNavDropdown>
        <EsaNavDropdown slot="end" align="end" icon="circle-user" label="Angela Zhao">
          <EsaLinkColumn heading="Account" links={[{ label: 'Dashboard' }, { label: 'Sign out' }]} />
        </EsaNavDropdown>
      </EsaAppBar>
    </Preview>
  </Section>

  <Section id="api" title="API">
    <p class="api-note">
      Props, types, defaults and events are <strong>generated from the component
      source</strong> and documented on the hub — this page shows the component
      wearing the ProjectFirma 2.0 skin.
      <a href="https://esassoc.github.io/ecology/components/esa-nav-dropdown" rel="noreferrer">ecology / components / esa-nav-dropdown</a>
    </p>
  </Section>
</ComponentDoc>
