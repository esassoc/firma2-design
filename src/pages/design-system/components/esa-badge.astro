---
import ComponentDoc from '../../../layouts/ComponentDoc.astro';
import Section from '@esa/docs/Section.astro';
import Preview from '@esa/docs/Preview.astro';
import ApiTable from '@esa/docs/ApiTable.astro';
import Badge from '@esa/ecology/esa-badge.astro';

// Prose only — names, types and defaults are generated from the component
// source by src/data/component-api.ts, so this table cannot drift.
const describe = {
  value: "Text or count shown inside the badge. Ignored in dot mode.",
  variant: "Background color intent.",
  size: "Badge height and text size.",
  dot: "Renders a small status dot with no text.",
};
---
<ComponentDoc name="Badge" category="Display" slug="esa-badge" type=".astro" status="stable"
  summary="A small pill for counts, statuses, and labels. Six color intents, three sizes, plus a dot mode for compact status indicators.">

  <Section id="preview" title="Preview">
    <Preview code={`<Badge value="New" />`}>
      <Badge value="New" />
    </Preview>
  </Section>

  <Section id="variants" title="Variants">
    <Preview code={`<Badge value="Primary" variant="primary" />
<Badge value="Secondary" variant="secondary" />
<Badge value="Success" variant="success" />
<Badge value="Warning" variant="warning" />
<Badge value="Danger" variant="danger" />
<Badge value="Info" variant="info" />`}>
      <Badge value="Primary" variant="primary" />
      <Badge value="Secondary" variant="secondary" />
      <Badge value="Success" variant="success" />
      <Badge value="Warning" variant="warning" />
      <Badge value="Danger" variant="danger" />
      <Badge value="Info" variant="info" />
    </Preview>
  </Section>

  <Section id="sizes" title="Sizes">
    <Preview code={`<Badge value="Extra small" size="xs" />
<Badge value="Small" size="sm" />
<Badge value="Medium" size="md" />
<Badge value="Large" size="lg" />`}>
      <Badge value="Extra small" size="xs" />
      <Badge value="Small" size="sm" />
      <Badge value="Medium" size="md" />
      <Badge value="Large" size="lg" />
    </Preview>
  </Section>

  <Section id="states" title="Dot mode">
    <p>With <code>dot</code>, the badge collapses to an 8px circle — useful as an inline status indicator next to a label.</p>
    <Preview code={`<Badge dot variant="success" />
<Badge dot variant="warning" />
<Badge dot variant="danger" />`}>
      <Badge dot variant="success" />
      <Badge dot variant="warning" />
      <Badge dot variant="danger" />
    </Preview>
  </Section>

  <Section id="api" title="API">
    <ApiTable slug="esa-badge" describe={describe} />
  </Section>

  <Section id="accessibility" title="Accessibility">
    <ul class="a11y">
      <li>Purely presentational — pair with adjacent visible text so the count or status has a label.</li>
      <li>Dot mode has no text content; convey its meaning through surrounding copy or an <code>aria-label</code> on the wrapper.</li>
      <li>Color is not the only signal where possible — prefer text counts over color alone.</li>
    </ul>
  </Section>

</ComponentDoc>
